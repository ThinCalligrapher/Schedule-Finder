#include <iostream>
#include <iomanip>
#include <cmath>

int main() {
    float v0, v, x0, x, t;
    float a = 9.8;

    std::cout << std::setw(30) << std::left << "Pick one equation" <<
    "\n" <<  "based on your known parameters" <<
    "of v0, v, x0, x, t" << std::endl;

    std::cout << "v = v0 + at" << "\n";
    std::cout << "";

    return 0;

}